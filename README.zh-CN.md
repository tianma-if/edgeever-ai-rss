# EdgeEver AI RSS

[English](README.md)

这是 [EdgeEver](https://github.com/tianma-if/edgeever) 官方插件。

以 AI 为核心的 EdgeEver RSS / Atom 阅读插件。它使用 EdgeEver 工作区已经配置的默认 AI Provider，为订阅内容提供翻译、总结、相关性筛选和推荐，并将有价值的内容沉淀为笔记。

## 功能

- 首次默认启用包含 18 个来源的“AI 前沿”，覆盖官方实验室、每日简报、研究分析、实践者写作、中文机器学习与深度访谈；另提供开发与开源、中文阅读、科学与研究、产品与设计、商业与创业、安全与隐私等可选主题。
- 并发读取精选 RSS / Atom 并缓存最近文章；网络或 AI 请求失败时仍可阅读已有内容。跨来源事件聚类会折叠规范化链接、规范化标题及高置信度相似标题，同时保留全部佐证来源。
- 支持由用户主动触发的单篇翻译、结构化总结和当前文章列表推荐。
- 刷新后批量自动翻译尚未处理的文章标题与订阅摘要；默认开启并以简体中文为目标语言，设置页会明确提示首次刷新可能产生多次工作区 AI 调用。另可选择繁体中文、英语、日语或韩语；正文翻译仍由用户主动触发。
- 按所选主题分别生成 RSS 日报，默认汇总最近 24 小时，标题采用 `YYYY-MM-DD · 分类 · RSS 日报`；候选文章会按规范化链接和标题去重、覆盖当前可用的来源角色，并限制单一来源占比后再交给 AI。同日同分类再次生成时更新原笔记，没有新内容的分类会跳过。每个有内容的主题调用一次 AI。
- 可由用户明确开启桌面端自动日报，并按设备时区从 24 个整点中选择生成时间；默认关闭，界面会在启用前说明 AI 调用方式，也可随时暂停。
- 通过 EdgeEver 官方声明式插件设置页统一配置订阅主题、打开时自动刷新、自动日报时间、日报时间范围和单分类文章上限；布局、校验、响应式与保存状态由 EdgeEver 统一提供。
- 将原文链接、摘要和 AI 结果保存为 EdgeEver 笔记。
- Provider、模型和凭据完全由 EdgeEver 管理。

## 开发

```sh
bun install
bun run check
bun run check:feeds # 可选：在线检查内置订阅源健康状态
```

构建会生成 EdgeEver 所需的 `main.js` 与 `styles.css`。发布 GitHub Release 时，应将它们与仓库根目录的 `manifest.json` 一同上传，并确保版本与标签一致。

插件通过 EdgeEver 的匿名只读网络传输获取公开订阅，不携带 Cookie、账号凭据或敏感请求头。当前界面提供经过验证的精选来源；后续可增加对任意公开 HTTPS RSS / Atom 地址的支持。

目标笔记本属于当前工作区的动态数据，因此继续在阅读功能面板中选择；普通偏好均放在 EdgeEver 统一的插件设置页中。

## 致谢

本项目的 RSS 阅读、订阅管理与文章收藏等产品思路受到 [Qiaomu AI RSS](https://github.com/joeseesun/qiaomu-ai-rss) 启发。扩充 AI 来源时参考了采用 MIT 许可证的 [QMReader 来源注册表](https://github.com/joeseesun/qmreader/blob/main/lib/sources.js) 作为候选池；本项目没有整表复制，而是逐个复核相关性、HTTPS 直连能力与解析兼容性后再纳入。

## 许可

MIT
